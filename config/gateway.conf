# ─────────────────────────────────────────────────────────────────────────
# High-Performance C++ Linux Network Gateway Configuration
# ─────────────────────────────────────────────────────────────────────────

# Network Server Settings
port = 8080

# Concurrency Thread Pool Size
thread_pool_size = 8

# Rate Limiter Quotas (Token Bucket Algorithm)
rate_limit_capacity = 200
rate_limit_refill_rate = 100

# Logging Configuration
log_level = DEBUG
log_file = gateway_runtime.log

# Session Security Credentials
shared_secret = qualcomm_production_s3cr3t_2026
